## 1. README 目录与锚点

- [ ] 1.1 在根 `README.md` 顶部新增“目录”区块，覆盖主要二级章节并为每个目录项分配稳定的 ASCII 锚点
- [ ] 1.2 为 `README.md` 中被目录覆盖的章节补充对应显式锚点，确保根文档与同步后的 `cloudflare/web/readme.md` 使用同一套导航目标
- [ ] 1.3 运行 `npm run sync:readme`，确认同步后的 `cloudflare/web/readme.md` 与根 `README.md` 的目录和锚点内容一致

## 2. 前端 ReadMe 跳转支持

- [ ] 2.1 在 `cloudflare/web/app.js` 的 `renderReadme()` 流程中加入锚点增强逻辑，保证 README 目录链接在前端页面内可跳转
- [ ] 2.2 处理带 `location.hash` 的首次进入场景，在异步渲染完成后自动定位到目标章节
- [ ] 2.3 补充最小必要的滚动偏移样式或 DOM 处理，避免目录跳转后标题被顶部布局遮挡

## 3. 测试与冒烟文档

- [ ] 3.1 更新 `tests/standards/smoke-test.md`，新增 ReadMe 目录跳转用例，覆盖“页面内点击目录跳转”和“带 hash 首次打开定位”两个场景
- [ ] 3.2 在 `.tests/` 下补充本次变更的本地验证记录或脚本，明确本地启动、点击目录、直接访问锚点链接的检查步骤
- [ ] 3.3 本地运行 `npm run dev`，验证 GitHub 风格目录内容已被同步到 ReadMe 页面，且目录点击与 hash 首次加载都能正确定位

## 4. 发布前检查

- [ ] 4.1 检查 `README.md` 新增目录后在仓库视图中可读、链接不失效，且没有引入第二份目录配置文件
- [ ] 4.2 检查 `npm run deploy` 发布链路仍会自动执行 `sync:readme`，确保生产环境与本地环境使用同一份目录内容
- [ ] 4.3 汇总发布前检查结果，记录是否更新冒烟文档、是否完成本地验证，以及是否可以进入正式部署
